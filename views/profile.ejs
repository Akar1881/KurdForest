<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %>'s Profile</title>
    <link rel="stylesheet" href="/css/global2.css">
    <link rel="stylesheet" href="/css/profile.css"> 
</head>
<body>
    <%- include('partials/header') %>

    <div class="profile-container">
        <!-- Profile Header -->
        <div class="profile-header">
            <div class="profile-picture">
                <img src="<%= user.profilePicture %>" alt="Profile Picture">
            </div>
            <div class="profile-info">
                <h1><%= user.username %></h1>
                <p><%= user.bio %></p>
                <% if (isOwnProfile) { %>
                    <a href="/watchlist/<%= user.username %>" class="btn">View Your Watchlist</a>
                <% } else { %>
                    <a href="/watchlist/<%= user.username %>" class="btn">View <%= user.username %>'s Watchlist</a>
                <% } %>
            </div>
        </div>

        <!-- Profile Edit (only for owner) -->
        <% if (isOwnProfile) { %>
        <div class="profile-edit">
            <h2>Edit Profile</h2>
            <form class="profile-edit-form" action="/profile/edit" method="POST" enctype="multipart/form-data">
                <div>
                    <label for="profilePicture">Change Profile Picture (Max 1MB):</label>
                    <input type="file" id="profilePicture" name="profilePicture" accept="image/*">
                </div>
                <div>
                    <label for="bio">Bio:</label>
                    <textarea id="bio" name="bio"><%= user.bio %></textarea>
                </div>
                <button type="submit">Save Changes</button>
            </form>
        </div>

        <div class="logout-section">
            <a href="/logout" class="logout-btn">Logout</a>
        </div>
        <% } %>
    </div>

    <%- include('partials/footer') %>
</body>
</html>