<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    <link rel="stylesheet" href="/css/global2.css">
    <style>
        .error-container {
            max-width: 600px;
            margin: 6rem auto;
            padding: 3rem;
            text-align: center;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(15, 163, 90, 0.2);
            border-radius: 12px;
            box-shadow: 0 10px 50px rgba(15, 163, 90, 0.2);
        }

        .error-container h1 {
            font-size: 6rem;
            font-weight: 800;
            color: var(--forest-green);
            margin-bottom: 1rem;
            text-shadow: 0 0 40px rgba(15, 163, 90, 0.6);
        }

        .error-container p {
            font-size: 1.2rem;
            color: var(--light-grey);
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .error-container a {
            display: inline-block;
            padding: 0.75rem 2rem;
            background: var(--forest-green);
            border: none;
            border-radius: 8px;
            color: var(--white);
            text-decoration: none;
            font-size: 1rem;
            font-weight: 600;
            transition: all 0.3s ease;
        }

        .error-container a:hover {
            background: #0d8a4d;
            box-shadow: 0 0 30px rgba(15, 163, 90, 0.6);
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .error-container {
                margin: 3rem 1rem;
                padding: 2rem;
            }

            .error-container h1 {
                font-size: 4rem;
            }

            .error-container p {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>

    <div class="error-container">
        <h1>404</h1>
        <p>The page you requested could not be found.</p>
        <a href="/">Go to Homepage</a>
    </div>

    <%- include('partials/footer') %>
</body>
</html>